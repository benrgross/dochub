import ListSlot from './page'

/**
 * Default content for the @list slot when the current route doesn't match
 * one of its page.tsx files (e.g. on hard refresh of a nested route).
 * We delegate to the unfiltered list page.
 */
export default ListSlot
